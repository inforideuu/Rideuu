import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rideuu.customer',
  appName: 'Rideuu Customer',
  webDir: 'dist/client'
};

export default config;
